{
  "steps": {
    "install": {
      "commands": [
        "python -m venv /app/.venv",
        "pip install -r requirements.txt"
      ]
    }
  },
  "deploy": {
    "cmd": "gunicorn statfort.wsgi --log-file -"
  }
}